[project]
name = "src"
version = "0.0.1"
description = "An RAG app for invoice processing"
authors = [{name = "learning-group", email = "abc"}]

[tool.setuptools]
packages = {find = {}}

[tool.setuptools.dynamic]
dependencies = {file = "requirements.txt"}